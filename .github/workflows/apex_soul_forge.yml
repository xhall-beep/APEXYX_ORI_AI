name: Apexyx Soul Forge
on:
  push:
    branches: [ sovereign-alignment ]
  workflow_dispatch:

jobs:
  forge:
    runs-on: ubuntu-latest
    container:
      image: kivy/buildozer:latest
      # Force the container to allow root or map to the buildozer user
      options: --user root
    steps:
      - uses: actions/checkout@v4

      - name: Fix Directory Permissions
        run: |
          chown -R root:root .
          # Ensure buildozer can create its own hidden folders
          mkdir -p .buildozer
          chmod -R 777 .buildozer

      - name: Atomic Spec Injection
        run: |
          cat << 'EOT' > buildozer.spec
          [app]
          title = Apexyx Soul
          package.name = apexyx_soul
          package.domain = org.montgomery
          source.dir = .
          source.include_exts = py,png,jpg,kv,atlas,js,so,dex,conf,sh,txt
          version = 0.1
          requirements = python3,kivy==2.3.0,requests,certifi
          orientation = portrait
          android.api = 33
          android.minapi = 21
          android.ndk = 25b
          android.arch = armeabi-v7a
          android.accept_sdk_license = True
          p4a.branch = master
          [buildozer]
          log_level = 2
          EOT

      - name: Sovereign User-Flow Strike
        run: |
          # The --allow-root flag is the key to bypassing the Kivy image block
          buildozer --allow-root android debug
        
      - name: Upload Sovereign APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Apexyx-Sovereign-APK
          path: bin/*.apk
